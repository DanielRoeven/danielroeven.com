import styled from 'styled-components'
import prims from '../Primitives'

const OlMedium = styled.ol`
	padding: 1.85rem 0 1.15rem 0;
	
	color: ${prims.colors.blue.dark.opaque};
	
	font-size: ${prims.fontsize.m};
	line-height: 3rem;
	
	list-style: none;
`

export default OlMedium
